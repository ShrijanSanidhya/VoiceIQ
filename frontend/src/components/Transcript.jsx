import React from 'react';

// Transcript Component
// Displays the speech-to-text output from Whisper
const Transcript = () => {
  return (
    <div>
      {/* TODO: Render transcription text, possibly with speaker diarization tags */}
      <div>Transcript placeholder</div>
    </div>
  );
};

export default Transcript;
